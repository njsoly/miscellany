# these are just some bash aliases that I like to have around.


# WINDOWS shorthands
alias npp='notepad++'
alias notepadpp='notepad++'

# MIMICRY OF CMD.EXE THINGS :: COMPATIBLE CROSS-PLATFORM #
alias path='echo $PATH $*'


####    MY BASH CUSTOMIZATIONS   ####
alias lS='ls -A --size --block-size=KiB'
alias lskb='ls -A -s --block-size=KiB'
alias fxn='function'

# GIT rapid-fire shit
alias gs='git status $*'
alias gsnu='git status -uno $*'
alias gsuno='git status -uno $*'
alias guncha='git update-index --assume-unchanged'
alias remotev='git remote -v'
alias remtv='git remote -v'
alias fetcha='git fetch --all'
alias ga='git add '
alias gau='git add -u'
alias pusha='git push --all $*'

####  A FEW ACCIDENTAL-CAPS MISTAKES TO OVERLOOK  ####
alias ALIAS='alias'
alias EXIT='exit'

